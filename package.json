{
	"name": "wikimedia-patched",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Apply unmerged Gerrit patches to live Wikimedia wikis.",
	"scripts": {
		"build": "node build.mjs",
		"test": "node --test",
		"smoke": "node scripts/gerrit-smoke.mjs"
	}
}
