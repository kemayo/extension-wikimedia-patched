{
	"name": "wikimedia-patched",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Apply unmerged Gerrit patches to live Wikimedia wikis.",
	"scripts": {
		"build": "node build.mjs",
		"test": "npm run build && node --test \"test/*.test.js\"",
		"smoke": "npm run build >/dev/null && node scripts/gerrit-smoke.mjs",
		"smoke:less": "npm run build >/dev/null && node scripts/less-smoke.mjs",
		"smoke:skew": "npm run build >/dev/null && node scripts/skew-smoke.mjs"
	},
	"dependencies": {
		"less": "^4.9.1"
	}
}
