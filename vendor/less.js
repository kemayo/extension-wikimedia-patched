/**
 * Placeholder. `npm run build` overwrites this with the real compiler when
 * `node_modules/less` is installed.
 *
 * Without it the extension resolves a patch stylesheet and its imports, but
 * cannot turn LESS into CSS. Plain CSS is unaffected.
 */
export default null;
